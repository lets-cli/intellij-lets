shell: bash

mixins:
  - lets.build.yaml
  - lets/lets.docs.yaml

commands:
  test:
    depends: [build]
    cmd: echo ${LETS_COMMAND_NAME}

  hello: echo Hello

  run:
    depends: [test, hello]
    cmd: |
      set -ex
      echo Run
      echo Rff
      if [ -n "${LE}" ]; then
        exit 1
      fi
